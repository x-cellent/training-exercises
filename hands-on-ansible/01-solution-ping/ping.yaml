---

- hosts: all
  gather_facts: false
  tasks:
    - name: ping the host
      ansible.builtin.ping:
      register: answer

    - name: print the outcome
      ansible.builtin.debug:
        var: answer
